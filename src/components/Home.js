import React, { Component } from 'react'

class Home extends Component {
  render() {
    const { primaryNavigation } = this.props

    return (
    <div>
      <h2>Accueil</h2>
    </div>
    )
  }
}

export default Home